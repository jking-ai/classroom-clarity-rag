services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: classroom-clarity-db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: classroomclarity
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
